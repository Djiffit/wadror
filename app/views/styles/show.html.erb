<p id="notice"><%= notice %></p>
<h2><%= @style.name %></h2>

<p><%= @style.description %></p>
<b><p>Beers</p></b>
<% @beers.each do |b| %>
<li>

  <td><%=link_to b.name, beer_path(b.id)%> by <%= link_to b.brewery.name, brewery_path(b.brewery_id)%></td>

</li>
<% end %>
<%= link_to 'Edit', edit_style_path(@style) %> |
<%= link_to 'Back', styles_path %>
